@model IEnumerable<CallejonDiagonApp.Models.Unidadesmedida>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AbreviaturaMedida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionMedida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MedidaStatus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AbreviaturaMedida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionMedida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MedidaStatus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdUnidadMedida">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdUnidadMedida">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdUnidadMedida">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
